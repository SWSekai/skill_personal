# {{PROJECT_NAME}} — Claude Code 專案規範

## 語言與格式
- Commit 訊息、修改日誌、README、對話回覆、UI 文字：**台灣繁體中文**
- **嚴禁使用中國大陸用語**，須使用台灣慣用詞彙：

| 禁用（中國用語） | 使用（台灣用語） |
|------------------|------------------|
| 視頻 | 影片 |
| 文件 | 檔案 |
| 信息 | 訊息 |
| 默認 | 預設 |
| 數據 | 資料 |
| 程序 | 程式 |
| 軟件 | 軟體 |
| 硬件 | 硬體 |
| 鏈接 | 連結 |
| 端口 | 連接埠 |
| 激活 | 啟用 |
| 打印 | 列印 |

- Commit prefix：`feat:`, `fix:`, `ui:`, `docs:`, `refactor:`
- 所有 commit 附加：`Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>`

## 核心行為規則

> **本檔範圍（強制）**：僅放跨專案通用協作原則。**專案特定資料策略**（樣本排除清單、評估 PDF 過濾、欄位定義、業務術語等）禁止寫入 — 改走「程式碼預設值（最強綁定）→ project memory → PROJECT_JOURNAL.md」。判斷檢核：「換一個專案這條規則還適用嗎？」否 → 不進本檔。

### 1. 每次修改前 — 影響評估
- 主動檢查改動對現有功能、架構的影響或衝突
- 主動提出潛在風險（向下相容、競態條件、資料遺失）
- 若無風險，明確說明「無潛在風險」

### 2. 每次修改後 — 品質檢查
- 掃描冗餘程式碼、死碼、未使用的 import
- 驗證是否符合專案現有設計模式
- 檢查 JSON 序列化風險

### 3. 每次 Commit 前 — 自動化日誌
- **必須**建立修改紀錄
- 必備欄位：日期時間、版本號、更動原因、檔案影響行數、影響範圍、容器重啟需求

### 4. Sekai_workflow/ 禁止加入專案版控，變更須推送至獨立倉庫
- `Sekai_workflow/` 已加入 `.gitignore`，**絕對不可**使用 `git add -f` 或任何方式加入專案版控
- `Sekai_workflow/` 屬於 `sekai-workflow` 遠端倉庫（`https://github.com/SWSekai/sekai-workflow.git`），僅透過該倉庫管理
- **每次修改 `Sekai_workflow/` 內容後，必須同步推送至遠端倉庫**

### 5. 每次 Commit 前 — .gitignore 安全檢查與狀態總覽
- 讀取 `.gitignore`，確認即將 stage 的檔案不在忽略清單中
- 列出所有待處理狀態：未暫存修改、已暫存未 commit、已 commit 未 push
- 向使用者確認後才執行 commit

### 6. 每次 Commit 後 — 容器重啟評估
- 根據改動檔案列出需重啟的容器及指令

### 7. README 維護
- 具備功能的資料夾下必須有 `README.md`
- 新增任何資料夾時，必須同步建立 `README.md`
- Commit 時若改動影響目錄結構或功能，主動更新對應 README

### 8. Skill 動態更新
- 當對話中出現新規則或偏好時，主動詢問是否更新至 Skill 規範
- 獲得確認後修改 `.claude/skills/` 對應文件

### 9. Memory、Skill、Sekai_workflow 三向連動（強制）
> **此規則為強制執行，不依賴 Skill 載入。每次寫入 Memory 時必須在同一回覆中完成以下所有步驟。**

- **步驟 1：寫入 Memory**
- **步驟 2：立即判斷歸類**（不可拆成下一回覆）
  1. 是否為可重複套用的**通用協作**行為約束或流程規則？ → **同步更新 CLAUDE.md**
  2. 是否具備跨專案通用性？ → **回流至 `Sekai_workflow/` 模板並推送遠端**
  3. 是否僅適用當前專案／屬於資料策略（樣本／檔名／業務術語）？ → **僅 Memory，禁止寫入 CLAUDE.md；綁定靠程式碼預設值（PDFS 清單 / filter / config 預設）**
- **步驟 3：若有對應 Skill**，同步更新 `.claude/skills/` 對應的 SKILL.md
- **步驟 4：若更新了 `Sekai_workflow/`**，commit & push 至遠端

**禁止**：僅寫入 Memory 就結束回覆、將同步留給下一回覆、等使用者提醒才補做。
**原因**：Memory 綁定專案路徑，僅存 Memory 的規則無法隨 Sekai_workflow 帶到新專案。

### 10. 新增 Skill 後 — 完整性檢查
- Skill 資料夾包含 `SKILL.md` 和 `README.md`
- `.claude/skills/README.md` 已更新
- `Sekai_workflow/` 對應資料夾已同步
- CLAUDE.md 可用 Skills 列表已更新
- **子目錄結構符合 Rule 19**：至少含 `references/`；`assets/` 僅放模板與範例資料（禁可執行檔）；`agents/` 與 `scripts/` 為選配（見 Rule 19 取捨）
- **SKILL.md 僅放對外合約**（子指令路由、使用方式、步驟概要）；細節規則 / 解析器邏輯 / 資料結構一律放 `references/`

### 11. 新專案自動初始化 Skill
- 進入新專案時，自動偵測並初始化 Skill 環境
- 觸發 `/skm sync` 執行完整初始化流程

### 12. Sekai_workflow 遠端同步與衝突處理
- 每次對話開始時自動同步
- 觸發 `/skm sync` 執行遠端同步流程

### 13. 資訊查詢時主動建立文件
- 系統架構／功能查詢 → 直接以對話回答（2026-05-06 起 `/ask info` 與對應 hook 已移除）
- 知識記錄 → 專案專屬走 `/team note`（`.hanschen/guides/`）；跨專案通用走 `/kb add`（`sekai-workflow/handbook/`）
- 資料流追蹤 → `/ask <field>`（保留結構化方法論）

### 15. Y/N 確認一律由 Tool Confirmation UI 處理（最高優先級）
- 所有只需 Y/N 回答的操作，**直接執行對應 tool call**，讓使用者透過 tool 權限介面（approve/deny）決定
- **禁止句型**：「需要檢查嗎？」「是否需要我...？」「要不要...？」「要我執行嗎？」「是否繼續？」
- 適用：commit、push、容器重啟、檔案操作、診斷檢查 — 全部直接執行
- 不適用：需從多個選項中選擇、需使用者提供額外資訊
- 違反此規則等同流程缺陷，與三向連動同等強制性

### 16. 對話中主動記錄關鍵資訊
- 對話過程中若經評估含有功能相關的關鍵資訊，主動寫入記錄檔
- 關鍵資訊範例：資料庫名稱、collection/table 名稱、待開發功能、API 端點、環境變數、業務規則、第三方服務等

### 17. 規劃產出必須透過互動式 Markdown 文件
- 當使用者請求產生**規劃、設計方案、改動清單、決策比較**時，**不得僅以 CLI 回覆結束**，必須於 `.hanschen/decision/` 或 `.hanschen/board/` 建立對應 Markdown 文件
- 文件需支援互動（核取方塊、預填建議值、現狀對比欄位、全域編號、**每區塊末尾 `補充說明` 欄位**），讓使用者直接於檔案中勾選／修改並於補充說明區自由填寫情境、理由、反例
- **【強制】分支問題必須拆成可勾選選項**：有條件分支的問題（例：「資料可否外送？」下有 A/B/C 三種可能答案），必須列成多個互斥 `[ ]` 子項讓使用者勾選，**禁止** `[ ] 條件說明` 後留白要使用者自填字串。自由填寫格式僅限純數值／字串輸入題
- CLI 回覆僅負責：告知文件路徑、摘要重點、等待使用者確認
- 使用者於文件中完成選擇後回覆「OK／完成」，Claude 重新讀檔、**解析補充說明欄位**、生成變更摘要、再執行實作
- **實作完成 → 原檔末尾 append 內嵌結案摘要 → 重命名 `CLOSED_` 前綴 → PROJECT_JOURNAL.md 寫一行索引**（流程見 17.4）
- **尚未勾選／尚未實作者不得重命名**：決策文件在使用者尚未勾選 A/B/C、或勾選後尚未進入實作前一律保留原名；僅在「實作完成」這個明確節點才觸發重命名。

#### 17.1 決策表 vs 白板 vs TODO vs PROJECT_JOURNAL — 四者用途互斥對照（2026-04-24 擴充）

> **「四方工具」= TODO / board / decide / journal**（對應 `/team todo` / `/team board` / `/team decide` / `/team journal`）。`/team report --daily` 的每日報告為 report 子指令的模式，**不是獨立的第五工具**。

| 特徵 | 決策表（`decision/`） | 白板（`whiteboard/`） | TODO（`TODO.md`） | PROJECT_JOURNAL |
|---|---|---|---|---|
| **核心問題** | 「要選 A、B 還是 C？」 | 「這主題討論到哪？」 | 「還有什麼要做？」 | 「專案做了什麼／為什麼？」 |
| **使用者動作** | 勾選選項 | 多輪對話補充 | 項目勾除 | 讀（不手寫） |
| **互斥分支選項** | ✅ 必須 | ❌ | ❌（已決定） | ❌ |
| **生命週期** | 實作完成 → `CLOSED_` | 結案 → `CLOSED_` | 常駐單檔 | **永久累積** append-only |
| **收斂方式** | 內嵌摘要於原檔末尾 + 重命名 + PROJECT_JOURNAL 索引 | 內嵌摘要於原檔末尾 + 重命名 + PROJECT_JOURNAL 索引 | 完成項移歸檔區 | 由 decide/board 結案自動 append 索引 |
| **自動觸發條件** | ❌ **禁止自動**（只能手動或鏈式從 board 轉 decide） | 諮詢類對話「**3 輪後仍未收斂**」自動開白板 | 使用者說「btw/順便/臨時想到」自動靜默入 Pending + 回覆末一句話提示 | board/decide closure 後自動 append 索引列 |
| **禁用情境** | 單行 bug fix、簡單 A/B（改走 AskUserQuestion）、可在對話即答的瑣事 | 一問一答、task 已清楚無需探索 | 需使用者拍板選項的事、>200 字或含分支問號的事 | 任何手動編輯（違反自動索引原則） |
| **過渡至他者時機** | 結案後實作項 → 自動 append 到 TODO（標註 from CLOSED_*） | 浮現互斥決策 → 在白板內開 decide 子文件、連結引用 | 項目含需拍板選項 → tag `@needs-decide` 另開 decide、decide 結案後 TODO 含連結 | 無（單向終點） |

- **決策表觸發**：2+ 互斥選項、多檔案／多模組影響、資源權衡、Skill／規則修改
- **白板觸發**：系統諮詢、架構探索（尚未有明確選項）、疑難排解多輪、範例分析
- **TODO 觸發**：已明確要做、單行可描述、執行路徑清楚、不需選項討論
- **未來嘗試項一律走 TODO**（2026-04-24 新增，跨專案同步規則）：使用者訊號如「以後想試 X」「未來可以做 X」「將 X 加入代辦」→ 直接寫入 `TODO.md`，**禁止**塞進既有決策表當 `[ ]` 選項（該項無互斥對手、非現在要決定）；若決策表已滯留此類選項，結案時主動搬至 TODO 並在原選項標 `~~~~（移往 TODO）`
- **TODO.md 缺檔自動建立**：第一次收到 TODO 類訊號且專案根無 `TODO.md` → Claude 主動建立（不需使用者額外確認；此為 Rule 17.1 合法產物）
- **PROJECT_JOURNAL 觸發**：由 decide/board 結案**自動**寫入，使用者不手動編輯
- **誤用懲罰**：把「需使用者決定的事」塞 TODO → 失去選項脈絡；把「單行待辦」開決策表 → 過度文件化；把「一次性決策」留在白板 → 累積過期資訊；把「未來嘗試項」塞決策表 → 降低決策表信噪比、TODO 機制被邊緣化

**完整規則與格式範例**：由專案 `.local/docs/README.md` 提供（建議每個專案首次建立 `.local/docs/` 時同步產出該導覽檔）。

#### 17.1.1 TODO「btw / 順便」自動捕捉規則

使用者在對話中出現「btw」、「順便」、「臨時想到」、「對了」等口語標記時：
1. **不中斷主要任務**
2. 以 `/team todo add <text>` 內部呼叫，自動 append 到 `.local/collab/TODO.md` 的 Pending 區塊
3. 若內容含 `@high` / `@low` tag → 依標註；否則預設中優先
4. **在回覆結尾以一句話提示**「已將『<簡述>』加入 TODO Pending」
5. 若該項 >200 字或含分支問號（「A 還是 B」）→ 加入 TODO 同時提示「此項適合升級為 /team decide，要開嗎？」
6. 不適用：使用者正明確拒絕（「不用記」「算了」）

#### 17.1.2 decide A/B 雙模式：AskUserQuestion vs 決策檔

面對 2-N 選項時 Claude 必須區分：

| 情境 | 工具 | 條件 |
|---|---|---|
| **簡單 A/B/C**，選項間**無需規格討論**（如顏色、preset、顯示偏好） | `AskUserQuestion` 直接問 | 選完即執行，對話紀錄即為決策歷史 |
| **複雜 A/B/C**，選項需要**權衡、預填值、現狀對比、補充說明** | 開 `decide` 檔 | 走 Rule 17 完整互動流程 |

**判斷啟發法**：
- 選項描述能 5 字內講清楚 → AskUserQuestion
- 需要列影響範圍、風險、預填推薦 → decide
- 使用者明示「幫我開決策檔紀錄」→ decide（使用者意圖優先）
- Claude 不確定時 → 預設 AskUserQuestion（可事後升級）

#### 17.1.3 board 3 輪收斂自動觸發

board 的「自動觸發」具體條件（2026-04-24 鞏固）：
- 對話進入諮詢／規劃／troubleshoot 性質
- **3 輪往返後仍未收斂**（未產出明確 TODO、未走 decide、未完成回答）
- → 自動建立 `.hanschen/board/YYMMDD_<topic>_board.md`

1-2 輪即收斂的小諮詢 → 不必開檔，對話回答即可。

#### 17.1.4 過渡情境處理流程

| 情境 | 處理 |
|---|---|
| **白板中浮現互斥決策** | 白板內開 decide 子文件（`decision/YYMMDD_<subtopic>_decision.md`），白板用連結引用，繼續探索。若白板只剩此議題 → rename 白板為 decide（b 備用路徑） |
| **TODO 項發現需架構決定** | TODO 項加 tag `@needs-decide` + 另開 decide；decide 結案後 TODO 項 append 連結到 CLOSED_decision.md |
| **decide 結案後仍有執行 TODO** | decide 結案時，變更清單中的「實作項」自動 append 到 TODO（標註 `from CLOSED_YYMMDD_<topic>_decision.md`） |
| **journal 與 daily report 分工** | journal = **永久索引**（board/decide CLOSED 歷史）；daily report（`/team report --daily`）= **當日活頁**（TODO/closure/commit/交接）。兩者不重複 |

#### 17.1.5 反例偵測（Advisory 模式）

Claude 偵測到以下反例時，**以 AskUserQuestion 提示**「這像是 X 誤用，建議改走 Y」；最終決定權在使用者（非攔截）：

1. 把需使用者拍板的事塞 TODO（「要用 Redis 還是 Memcached」不應進 TODO）
2. 單行 bug fix 開 decide 檔（過度文件化）
3. 白板開了卻只有一問一答（應用 TODO 或對話回答）
4. PROJECT_JOURNAL 被手動編輯（違反自動索引原則）
5. 每日報告內容涵蓋跨週資料（應走 `/team report weekly` 或 `/team report YYMMDD YYMMDD`）

#### 17.1.6 brief 用字已剔除（2026-04-24）

早期設計曾用「brief」指稱每日 Teams 報告；此用字已剔除。替代術語：
- 舊 `YYMMDD_brief.md` → 新 `YYMMDD_daily_report.md`
- 舊 `references/daily-brief.md` → 新 `references/daily-report.md`
- 舊 `assets/brief-template.md` → 新 `assets/daily-report-template.md`
- 舊「daily Teams brief / Daily Brief」稱呼 → 「daily Teams report / Daily Report / 每日報告」

實際工具仍為 `/team report --daily`；brief 不再是獨立概念，而是 report 的一個模式。

#### 17.1.7 未來嘗試項一律走 TODO（2026-04-24 新增，跨專案同步）

使用者訊號如「以後想試 X」「未來可以做 X」「將 X 加入代辦」「之後可以試 X」→ 直接寫入 `TODO.md` Pending 區塊，**禁止**塞進既有決策表當 `[ ]` 選項。

**理由**：該項無互斥對手、非現在要決定；塞決策表會降低信噪比，使 TODO 機制被邊緣化。

**清理既有誤放**：結案時若決策表含此類滯留項 → 搬至 TODO，原選項標 `~~~~（移往 TODO）` 保留歷史。

**判斷準則**：
- 「現在需要在 A/B/C 中抉擇」→ decide
- 「以後想試 X」「未來再說 X」→ TODO
- 邊界不清時：選項是否互斥 → 互斥進 decide、可共存進 TODO

#### 17.1.8 TODO.md 缺檔自動建立（2026-04-24 新增）

第一次收到 TODO 類訊號（含 §17.1.1 btw/順便、§17.1.7 未來嘗試項等），若 `.local/collab/TODO.md` 不存在 → Claude **主動建立**含基本骨架（`## Pending / ## In Progress / ## Completed`），不需使用者額外確認。

#### 17.2 適用／不適用情境
- **適用（必須開決策表或白板）**：多檔案重構方案、新功能設計、架構／技術選型、Skill 與規則流程修改
- **不適用**：單檔 bug fix、單次資訊查詢、已在 TODO 清單中的執行項目、純程式碼修改

#### 17.3 複選題必附「全部都做」選項
- 決策表中的**複選題**（非互斥、可同時勾多項），若各選項間無資源衝突、無實作成本壓力，Claude **必須**在最後一項附上 `★ 全部都做`（等同全選）
- 目的：降低使用者逐項勾選的認知負擔；「都做」是常見最終意圖，預設提供省事路徑
- **不適用**：單選題、互斥選項組、各選項有顯著資源／時間權衡需個別取捨的情境
- 違反此規則：複選題未附「都做」、強迫使用者勾每一項

#### 17.4 決策／白板結案流程 — 原檔自足 + PROJECT_JOURNAL 索引（2026-04-22 改版；2026-05-13 加增量進度機制）

**核心原則**：結案摘要**內嵌於原檔末尾**（使單檔自給自足），PROJECT_JOURNAL.md 既作為**結案索引**（一行一決策，連回 `CLOSED_*` 原檔），也接受**增量 progress entry**（TODO 子項完成時寫入，narrative 形式）。**不再產出 `.local/docs/summary/` 獨立摘要檔**；該目錄已於 2026-04-22 刪除。

**原檔內嵌摘要必須五要素齊全**：背景 / 最終決策（逐項目 §n.m）/ 變更清單 / 🔖 保留候選（僅非單一路線）/ 未解決遺留項。統一模板見 `.claude/skills/team/references/naming.md` §5.2。

**PROJECT_JOURNAL.md 雙重定位**：
1. **結案索引（origin）**：跨決策概覽，每筆單列，欄位為 日期 / 主題 / 一句話決策摘要 / 連結 `CLOSED_*.md`
2. **增量 progress entry（2026-05-13 新增）**：narrative 形式，TODO 子項完成時寫入，記錄該 session 的方法／結果／變更，與既有 narrative entries 同格式

#### 17.4.1 增量進度寫入循環（2026-05-13 新增 — 大型 decide 每日可回報）

避免 decide 範圍過大導致當天無進度可回報。引入「decide → TODO → 子項完成寫 journal → /commit-push → 下一子項」循環。

**循環單元**（decide 中每個可獨立執行的 TODO 子項）：
1. decide 互動完成 → AI Agent 解析勾選 → 對應實作項列入 TODO Pending（含執行指令／預期收益）
2. TODO 子項執行中
3. **執行中途若發現新路徑（修正／更新）** → 補入 TODO Pending，繼續執行（不重開 decide）
4. 子項實作完成 → TODO 該項勾選並移至 Completed（`YYYY-MM-DD HH:MM — 事項摘要` 格式）
5. **同回覆內** append 一筆 progress entry 至 PROJECT_JOURNAL.md（narrative：背景／方法／結果／變更清單／遺留項／來源）
6. 觸發 `/commit-push`（或 `--meta` 視變更性質）收尾與統整當階段
7. 回到步驟 2 處理下一 TODO 子項，直到 decide 列出的子項全部完成

**decide CLOSED_ 條件（升級為「自然收斂」）**：
- 所有 decide 列出的 TODO 子項皆完成（含執行中途新增的）
- 此時 decide CLOSED_ 不再需要重複大規模 narrative — 直接 append 結案摘要 + 重命名 + PROJECT_JOURNAL append 單行索引指向 CLOSED_
- 因各 progress entry 已記錄「做了什麼／為什麼」，CLOSED_ 結案摘要可精簡為「五要素 + 連結各 progress entry 日期」

**Progress entry 格式**（narrative，與既有 entries 一致）：
- header：`### YYYY-MM-DD — <主題簡述>`
- **背景**：承接 decide §X 的什麼子項、為什麼需要這次 session
- **方法／結果**：本次 session 做了什麼、量化收益（指標前後對比表）
- **變更清單**：檔案級表格
- **🔖 保留候選**（若有）
- **遺留項**：尚未處理但已知的後續工作
- **來源**：指向 decide 檔 + 對應 §

**禁止**：
- 在 TODO 子項尚未完成（未勾選 Completed）前手動 append progress entry
- 為「規劃」「打算做」寫 entry — 必須是「已完成」才寫
- 跳過 /commit-push 直接開下一輪（破壞 commit 與 journal 的 1:1 對應）

**例外**：
- 純文件／規則／流程變更 → /commit-push 帶 `--meta`，journal entry 仍照寫
- Decide 子項只有一個（單步完成全部） → 退回原 17.4 結案流程，不必走增量
- 同一 commit 涵蓋多個 TODO 子項 → 合併寫一筆 progress entry 列舉各子項

**決策表結案**（`/team decide` Step 6 強制流程）：
1. 所有 TODO 子項完成（過程中已寫多筆 progress entries）
2. **於原決策檔末尾 append 內嵌結案摘要**（五要素齊全；若 17.4.1 進程已寫過 progress entries，此處摘要可精簡並列出涵蓋的 entry 日期區間）
3. 重命名原決策檔：`decision/<topic>_decision.md` → `decision/CLOSED_<topic>_decision.md`
4. **在 PROJECT_JOURNAL.md 決策紀錄表 append 一行結案索引**（連結指向 `CLOSED_*_decision.md`，補充 progress entries 日期區間）
5. 若內嵌摘要含「🔖 保留候選」→ 同步 append 至 PROJECT_JOURNAL 保留候選表
6. 更新 PROJECT_JOURNAL.md「最後更新」時間戳

**白板結案**（`/team board` Step 3 強制流程）：
1. 討論結束
2. **於白板檔末尾 append 內嵌結案摘要**（五要素，統一模板同決策表）
3. 重命名：`whiteboard/YYMMDD_<topic>_board.md` → `whiteboard/CLOSED_YYMMDD_<topic>_board.md`
4. **在 PROJECT_JOURNAL.md 討論成果表 append 一行索引**（連結指向 `CLOSED_*_board.md`）
5. 更新 PROJECT_JOURNAL.md「最後更新」時間戳

**`CLOSED_` 前綴為什麼不是刪除**：
- `CLOSED_*.md` = **原檔互動歷史 + 末尾內嵌五要素摘要**（單檔自足，給需要鑽取的人與新 session 用）
- PROJECT_JOURNAL.md = **跨決策索引表**（快速瀏覽專案做過哪些決定，點進去讀詳情）
- 兩者分工：索引快查「有哪些決定」，原檔回答「做了什麼／為什麼／過程中考慮過什麼」
- 使用者明示「刪除」才刪；預設動作是重命名保留

**保留候選規則**：
- 單一路線區塊（互斥選項）→ 只記最終採納項，不保留未選項
- 非單一路線區塊（可複選）→ 內嵌摘要中以「🔖 保留候選（未採納但可重啟）」列出未選項 + 未採納原因 + 重啟時機
- 候選超過 6 個月未重啟或被新決策取代 → 標註「已作廢」，**不刪除**（歷史紀錄）

### 19. Skill Reference 結構強制（2026-04-22 新增）

所有 `.claude/skills/<skill>/` 必須採用 **reference 分離**架構：SKILL.md 只放對外合約，細節規則拆到 `references/` 子目錄。對齊 Claude Code 官方 `skill-creator` 架構。

#### 19.1 子目錄職責

| 子目錄 | 必選/選配 | 內容 |
|---|---|---|
| `references/` | **必選** | 細節規則 `.md`，Claude 按需 Read |
| `assets/` | 選配 | 模板 + 範例資料（禁可執行檔） |
| `agents/` | 選配（延遲導入） | Agent 派遣 prompt 模板 |
| `scripts/` | 選配（有條件） | 可執行邏輯 — skill 專屬工具才建；跨 skill 可執行放 `.claude/hooks/` |

#### 19.2 SKILL.md 對外合約包含

- frontmatter（name, description, argument-hint, allowed-tools）
- Subcommand routing（總覽表）
- Common rules（跨子指令共用規則）
- 各子指令：Usage + Steps 概要（細節 reference `references/<detail>.md`）

#### 19.3 禁止放於 SKILL.md（必須移至 references/）

- 解析器邏輯 / regex 規則
- 資料結構定義
- 多頁流程細節（> 3 個 nested sub-step 就該拆出）
- Template 字串（歸 `assets/`）

#### 19.4 舊 skill 遷移

新建 skill 強制採用；舊 skill 只在改動時順手拆（不為拆而改動穩定 skill）。

#### 19.5 Reference 檔語言

與對應 SKILL.md 同語言。Output template 範例內的文字可保留專案語言。

### 20. Skill 新增流程收尾規範（2026-04-24 新增）

Skill 新增屬於**元層（meta）維護**而非專案正式貢獻，commit 必須與產品功能 commit 區隔。

#### 20.1 `/skm new` 最終步驟（強制）

`/skm new` Step 10 二擇一：
- **方案 A（預設推薦）**：`AskUserQuestion` 詢問是否立即 commit → 選 yes 內聯執行 `/commit-push --meta`
- **方案 B（進階）**：直接呼叫 `/commit-push --meta`（無互動）

不得以純文字 Y/N 取代 AskUserQuestion（違反 Rule 15）。

#### 20.2 `/commit-push --meta` flag 行為

`$ARGUMENTS` 含 `--meta` 時：
- **跳過 Step 5**（modify_log 建立）
- **跳過 Step 11**（daily report append）
- 保留所有其他 step
- commit message 建議前綴：`chore(skill):` 或 `docs(skill):`

#### 20.3 使用時機

`--meta` 適用：`/skm new`、`/skm update/sync`、CLAUDE.md / manifest.json / hooks 修改、Sekai_workflow 結構改動。
`--meta` 不適用：專案產品功能、bug fix、測試、使用者可見行為改動。

#### 20.4 灰區

同一次 commit 混合功能 + skill 維護：**優先拆兩次**；無法拆則走正常 `/commit-push`（功能優先）。

### 21. 修改前協議（Pre-modification Protocol，2026-04-24 新增）

**所有修改**（code、skill、rule、meta 皆適用）在動手前三步驟：

#### 21.1 遠端檢查

```bash
git fetch origin
git rev-list --left-right --count origin/main...HEAD
```
- 遠端有新 commit → 暫停、提醒 `git pull`（或 rebase / 新 branch 視分歧類型）
- 雙向分歧但 tree 相同（如 author rewrite）→ 可 rebase 對齊
- 同時檢查所有待改動 repo（含 Sekai_workflow 若獨立 git）

#### 21.2 風險評估

列出：
- 影響範圍：所有將改動檔案（skill / runtime mirror / template / manifest / hooks / memo）
- 潛在 Bug：命名衝突、歷史引用漂移、muscle memory 失效、hook regex 失效、多語 README 未同步
- 不可回復動作：目錄 rename / git reset / 檔案刪除 / runtime 覆蓋 → 明確列入 AskUserQuestion

#### 21.3 使用者確認

- `AskUserQuestion`（不用純文字 Y/N，違反 Rule 15）
- 選項區分：低風險接受預設 / 高風險需拆步 / 取消改走他案

#### 21.4 適用

專案程式碼、`.claude/skills/` runtime、`sekai-workflow/` 源、CLAUDE.md / template / manifest / hooks、跨 skill 重構、推送遠端操作。

#### 21.5 不適用

純讀取（grep/read/glob/list）、AskUserQuestion 本身。

#### 21.6 與 Rule 15 關係

Rule 15 規 Y/N → Tool UI；Rule 21 在 Y/N 前做「遠端檢查 + 風險評估」，兩者互補。

### 22. 實作請求規劃優先（Planning-First）

使用者提出實作需求時，開始撰寫程式碼前：
1. 列出實作步驟概要
2. 提供**至少兩套方案**（例：快速實作 vs 擴充性優化），各附 trade-off
3. 等待使用者確認方案後才進入實作

方案呈現工具（依 Rule 17.1 四方工具互斥對照）：
- 5 字內可講清楚 → `AskUserQuestion`
- 需權衡/預填/現狀對比 → `/team decide`
- 多輪探索未收斂 → `/team board`
- 未來嘗試項 → `/team todo`（**不要**塞進方案表）

不適用：單行 bug fix、使用者明示「直接做」、客觀上只有單一合理方案（須說明）。

### 23. 非 skill 頂層目錄變更時必檢基礎設施腳本（2026-04-24 新增）

新增、改名、刪除 `sekai-workflow/` 頂層**非 skill 目錄**（如 `handbook/`、`docs/`、`hooks/`、`references/`）時，必須檢查並更新：

| 腳本 | 檢查點 | 漏檢後果 |
|---|---|---|
| `_bootstrap/sp-init.bat` | `:CopySkill` skip list | 目錄被誤拷貝到 `.claude/skills/` |
| `_bootstrap/sp-sync.sh` | `SKIP_DIRS` 變數 | sync log 異常（雖有 `SKILL.md` 檢查保底）|
| `_bootstrap/sp-pack.sh` | preserve-before-delete 邏輯 | `rm -rf $SP_DIR` 遺失未 push 資料 |
| `_bootstrap/sp-verify.bat` | 驗證清單 | 缺乏關鍵基礎設施驗收 |
| `manifest.json` | `skills` / 資料區分類 | 目錄歸類錯誤 |

**判斷**：目錄是否含 `SKILL.md`？無 → 非 skill → 落入此規則。
**歷史教訓**：2026-04-24 新增 `handbook/` 時漏檢 → 事後補救催生此規則。

### 24. 每次接收新請求 --- 主動建立 TaskCreate TODO（2026-05-06 新增）
- 任務涉及 **3 個步驟以上**，或使用者一次提出複數需求時，**開始執行前**先用 TaskCreate 建立任務清單
- 單一步驟的直觀任務可直接執行，無需建 TODO
- 目的：避免 context 清空後遺失工作目標，讓使用者隨時看到進度

### 25. `.hanschen/` vs `.local/` 邊界（2026-05-07 新增；2026-05-08 路徑扁平化）

- **`.hanschen/`**：跨機器共享文件（入專案 git，可選擇性 push）— `decision/`、`board/`、`journal/`、`guides/`、`handoff/`、`modify_log/`、`report/`、`.history/`；2026-05-08 起取消 `.hanschen/docs/` 中介層，所有子目錄皆於 `.hanschen/` 根層
- **`.local/`**：機器專屬狀態（不入版控）— `context_summary/`、`docs/plan/`（實作計畫）、`bag/`（pack 輸出）、`model_dispatch.log`、`resumption_prompt.md`
- **互斥原則**：兩者不互為 fallback，禁止「找不到就退到另一邊」的查找行為
- **完整邊界表 + `/team sync` 遷移規格**：見 `.hanschen/.history/README.md` §1～§5（權威定義）

### 26. `--no-subagent` flag 跨 skill 標準（2026-05-08 新增；2026-05-13 擴及全 Skill）

**強制（Mandatory）**：所有會 dispatch Agent 子任務的 skill **必須**支援 `--no-subagent` flag，作為使用者規避 Opus 1M-context API 閘道、增加單一 transcript 稽核軌跡、或除錯時的逃生口。

**聲明式（Declarative no-op，2026-05-13 擴及）**：不 dispatch Agent 的 skill **亦須**在 SKILL.md / README.md 揭露此 flag 為 no-op（接受但無作用）。理由：跨 skill 一致性 + 為未來新增 Agent dispatch 預留無痛升級空間。

#### 適用 skill

| Skill | 採用狀態 | 影響子命令／備註 |
|---|---|---|
| `/commit-push` | ✓ 強制 | Step 1（Opus 品質）、Step 5（Haiku 修改日誌）|
| `/build` | ✓ 強制 | `plan`（Opus）、`check`（Opus）、`all` 串接時繼承 |
| `/team` | ✓ 強制 | `report --daily`（Haiku）、`handoff`（Opus）、decide/board 結案摘要（Opus）|
| `/skm` | ✓ 強制 | 各子命令的偶發 Agent 呼叫（多為安全保護）|
| `/hello` | ✓ 聲明式 no-op | 不 dispatch Agent；flag 為一致性而接受 |
| `/clean` | ✓ 聲明式 no-op | 不 dispatch Agent；可與 `check` / `force` 組合 |
| `/ask` | ✓ 聲明式 no-op | 目前不 dispatch；`allowed-tools` 含 `Agent` 為未來預留 |
| `/kb` | ✓ 聲明式 no-op | 不 dispatch Agent；add/search/extract 皆於主 session 完成 |
| `/memo` | ✓ 聲明式 no-op | 不 dispatch Agent；多數為自動觸發 |
| `/dispatch` | ✗ 例外 | 本身為 dispatch 機制，flag 不適用 |

#### 行為規範

1. flag 出現於 `$ARGUMENTS` → skill 內所有 `Agent(...)` 呼叫改為主 session inline 執行
2. model 切換不發生（主 session model 固定）
3. 步驟邏輯不變，僅去除子代理產生機制
4. chain 繼承：`/build all --no-subagent` 串接的 `/commit-push` 自動帶入此 flag
5. 可組合：`--meta --no-subagent` 等與其他 flag 並用
6. **聲明式 no-op**：skill 不 dispatch Agent 時，flag 為 no-op；SKILL.md 與 README.md 仍須揭露以維持一致性

#### 觸發時機

- **使用者顯式呼叫 skill（最高優先級，2026-05-13 新增）**：當使用者輸入 `/<skill>` 或 `/<skill> <subcommand>` 時，Claude **預設自動帶 `--no-subagent`** 規避 1M context bug；Claude 自身鏈式呼叫（如 board 結案自動觸發 `/team report --daily`）不自動加。例外：使用者明確聲明要跑完整 sub-agent 流程。
- 主對話為 Opus 4.7 1M context（`[1m]` 後綴）且 `/extra-usage` 未啟用
- 過往執行中斷於 `API Error: Extra usage is required for 1M context`
- 使用者要求單一 transcript 稽核軌跡

完整實作模板見 `commit-push/SKILL.md` § `--no-subagent` Flag。違反此規則 = skill 在 1M context 下無法使用，等同流程缺陷。

### 27. 規則異動後檢核清單回填（2026-05-13 新增）

新增或修改 `CLAUDE.md`、`_bootstrap/templates/CLAUDE.md.template`、或任一 Skill 行為合約時，**必須**評估並執行以下回填清單。違反即流程缺陷，於 `/commit-push` Step 1.6 強制阻擋。本規則為其他散落 Rule（9 / 19 / 23 / 24）的傘狀統整入口，補齊治理閉環。

#### 27.1 觸發時機

- CLAUDE.md 規則新增 / 修改 / 廢除
- `sekai-workflow/_bootstrap/templates/CLAUDE.md.template` 對應修改
- Skill `SKILL.md` frontmatter 變動（description / argument-hint / allowed-tools）或步驟結構大幅變更

**不觸發**：純文字 typo、範例片段微調、標點/排版調整。

#### 27.2 必查項目（依改動性質擇要勾選）

| 改動類型 | 必查項目 |
|---|---|
| 路徑遷移 / 指令改名 | append `.hanschen/.history/refactor.jsonl` 一行 |
| 跨 skill 行為改變 | 更新雙方 SKILL.md / README.md 的 `Cross-Skill References` 區塊（Rule 24）|
| 新增 sync 偵測項 | 更新 `/team sync --audit` 偵測邏輯 |
| 影響 commit 前檢查 | 更新 `/commit-push` Step 1.6 整合性檢查 |
| 規則層級擴及全 skill | 同步更新 `_bootstrap/templates/CLAUDE.md.template`（給其他專案）|
| 影響 hook 行為 | 更新 `.claude/hooks/*.cjs` 與對應 settings.json matcher |

#### 27.3 強制執行點

`/commit-push` Step 1.6 擴充為「規則 / Skill 整合性檢查」：偵測本次 commit 是否含 `CLAUDE.md` / `CLAUDE.md.template` / `SKILL.md` 異動，若有則顯示 27.2 checklist，任一項未處理且無「不適用」聲明則阻擋 commit。

#### 27.4 失敗回滾

commit 後才發現未回填 → 補上、新開 commit（不要 amend）、commit message 加 `(rule-27 followup: <item>)` 標記。

### 28. 雙 clone rebase 反模式預防（2026-05-13 新增）

跨 clone 各自 rebase 同一分支會產生「訊息相同 hash 不同」的雙生 commit，導致 git 分歧不可自動收斂。每次 `/hello` 與 `/commit-push` 必須先做分歧偵測。

#### 28.1 偵測指令

```bash
git fetch origin --quiet
BRANCH=$(git rev-parse --abbrev-ref HEAD)
git rev-list --left-right --count origin/$BRANCH...HEAD 2>/dev/null
```

回傳格式 `<remote_ahead> <local_ahead>`：
- `0 N` / `N 0` → 正常單向領先
- 兩側 > 0 → **分歧**，啟動警示

#### 28.2 警示 threshold

| 兩側 commit 數 | 行為 |
|---|---|
| `1-2` 兩側都有 | 提示但不阻擋 |
| `3+` 兩側都有 | **強制提示**，列出處理方案：rebase / reset 對齊遠端 / 新分支隔離 |
| `10+` 兩側都有 | **阻擋** commit，要求使用者顯式解決 |

#### 28.3 強制執行點

- **`/hello` Step 0**：每次新對話開頭跑分歧偵測
- **`/commit-push` Step 0**：commit 前再跑一次

#### 28.4 不適用情境

- 本地分支未 push（`origin/<branch>` 不存在）→ skip
- 偵測結果 `0 0` → silent pass
- 偵測指令本身失敗 → silent skip

#### 28.5 顯式 override

`/hello --skip-divergence-check`、`/commit-push --skip-divergence-check` — commit message 註記 `(rule-28 skipped: <reason>)`。

## 可用 Skills

合併版（17 → 6），透過子命令路由至原 Skill 對應職能。

- `/build <flow|plan|impl|test|commit|quality|log|restart|eval>` — 開發全流程：需求分析 → 方案設計 → 實作 → 測試 → 品質檢查 → commit → push → 重啟評估
- `/team <todo|board|decide|notes>` — 互動協作：AI TODO、即時白板、Markdown 決策表、技術筆記
- `/skm <new|sync|pack|update>` — Skill 管理（2026-04-24 由 `/skill` 改名，避免與內建 `/skills` 衝突）：建立新 Skill、遠端同步、專案打包、更新捕捉
- `/kb <add|search|extract>` — **Knowledge Base 管理**（2026-04-24 新增）：維護 `sekai-workflow/handbook/` 跨專案工程手冊（Docker/K8s/ETL/算法/後端），`/team` 結案自動抽取 + 技術提問自動增強
- `/ask <query|trace|report>` — 系統文件與報告：系統資訊、資料流追蹤、工作報告
- `/clean [check|force]` — Context 清理入口（繼承 context-guard 功能）：清理舊摘要後執行 /clear，Stop hook 自動提醒
- `/memo` — Memory 跨專案攜帶
